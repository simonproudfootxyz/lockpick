# Lockpick

Solo card game built with React.

## Development

```bash
npm install
npm start
```

The app runs on `http://localhost:3000`.

## Testing

```bash
npm test
```

## Production Build

```bash
npm run build
```

Serve the `build/` folder with any static file host.
